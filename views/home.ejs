<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Swan Migration - Home</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <div class="auth-container">
            <h1>🦢 Swan Migration Patterns</h1>
            <p class="subtitle">Discover the incredible journeys of swans across continents</p>
            
            <% if (isAuthenticated) { %>
                <div class="user-info">
                    <p>Welcome back, <%= userInfo.email || userInfo.username %>!</p>
                </div>
                <a href="/swans" class="button primary">Explore Swan Migration</a>
                <a href="/logout" class="button secondary">Logout</a>
            <% } else { %>
                <div class="auth-message">
                    <p>Sign in to discover the fascinating world of swan migration patterns</p>
                </div>
                <a href="/login" class="button primary">Sign In</a>
            <% } %>
        </div>
    </div>
</body>
</html>